# Phase 12: Global Packaging and Distribution - Plan

## Goal
Transform the repository into a globally accessible package (e.g., `npx`) to maximize reach.

## Tasks
1. Create `package.json` with a bin command `cuda-aes-bench`.
2. Implement `bin/run.js` to compile via CMake and execute the benchmark.
3. Update `README.md` to instruct users on how to use `npx`.
