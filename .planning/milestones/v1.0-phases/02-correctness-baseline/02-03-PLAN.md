---
phase: 02-correctness-baseline
plan: 03
type: execute
wave: 3
depends_on: ["02-01", "02-02"]
files_modified: ["README.md", "CMakeLists.txt", ".planning/codebase/TESTING.md"]
autonomous: true
requirements: [TEST-04, TEST-05]
user_setup: []
must_haves:
  truths:
    - "TEST-04: Small smoke/correctness checks are documented and do not require benchmark-sized allocations."
    - "TEST-05: Maintainers and readers can see correctness status before trusting benchmark output."
    - "Benchmark docs must not imply untested GCM/AES behavior beyond the Phase 2 supported shape."
  artifacts:
    - path: "README.md"
      provides: "Correctness command and status notes"
      contains: "ctest|--kat|known-answer|GCM"
    - path: ".planning/codebase/TESTING.md"
      provides: "Updated codebase test map"
      contains: "known-answer"
  key_links:
    - from: "README.md"
      to: "correctness tests"
      via: "documented command"
      pattern: "ctest|--kat"
---

<objective>
Document and expose correctness status before benchmark claims.

Purpose: After Phase 2 fixes, outside users need an obvious way to run correctness checks and understand what has been proven. Benchmark credibility depends on this being visible before throughput tables.
Output: README/build/test documentation that points to the small correctness path and states current AES/GCM correctness scope.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-correctness-baseline/02-RESEARCH.md
@.planning/phases/02-correctness-baseline/02-VALIDATION.md
@.planning/phases/02-correctness-baseline/02-01-PLAN.md
@.planning/phases/02-correctness-baseline/02-02-PLAN.md
@README.md
@CMakeLists.txt
@.planning/codebase/TESTING.md
</context>

<threat_model>
Documentation risks:
- Users may trust benchmark output without running correctness checks.
- GCM scope may be overstated if only empty-AAD, 96-bit-IV vectors are covered.
- Local build limitations can hide whether tests were actually run.

Mitigations:
- Put correctness commands near build/run instructions.
- State exactly which modes/key sizes are covered by KATs.
- Keep any environment-limited verification notes explicit in summaries.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Document correctness command in README</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/02-correctness-baseline/02-RESEARCH.md</read_first>
  <action>
Add a concise correctness section to README that tells users how to configure/build and run the small KAT/smoke checks. Mention CTest if registered, or `CudaProject --kat` if that is the implemented path. Place the section before benchmark usage or clearly before any performance interpretation.
  </action>
  <verify>rg "Correctness|known-answer|ctest|--kat|smoke" README.md finds the section and command.</verify>
  <acceptance_criteria>
    - README includes a copy-paste correctness command.
    - README states checks are small and separate from the benchmark loop.
    - README does not require private local knowledge.
  </acceptance_criteria>
  <done>Users can find and run correctness checks before benchmarking.</done>
</task>

<task type="auto">
  <name>Task 2: State verified mode/key coverage and limitations</name>
  <files>README.md, .planning/codebase/TESTING.md</files>
  <read_first>README.md, .planning/codebase/TESTING.md, .planning/phases/02-correctness-baseline/02-02-SUMMARY.md</read_first>
  <action>
Update README and the codebase testing map to state which modes/key sizes have known-answer coverage after Phase 2: ECB-128/256, CTR-128/256, and GCM-128/256. For GCM, state the supported Phase 2 scope precisely, such as 96-bit IV and empty AAD if that is what was implemented. Do not claim production cryptographic library status.
  </action>
  <verify>rg "ECB-128|ECB-256|CTR-128|CTR-256|GCM-128|GCM-256|96-bit|AAD|production" README.md .planning\\codebase\\TESTING.md finds coverage and caveats.</verify>
  <acceptance_criteria>
    - Correctness status is visible in README.
    - GCM limitations are explicit and not buried.
    - Testing map no longer says there are no known-answer tests once Plan 01/02 add them.
  </acceptance_criteria>
  <done>Documentation matches the implemented correctness evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Final source-level verification and GSD state readiness</name>
  <files>.planning/STATE.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</read_first>
  <action>
Run final static checks for Phase 2 and record any environment-limited build/test result in the summary. Do not mark Phase 2 execution complete manually; let `$gsd-execute-phase` completion workflow update requirements after execution. Ensure the final summary tells the next phase whether correctness tests actually ran or were blocked by local CUDA host compiler setup.
  </action>
  <verify>git status --short shows only intended Phase 2 execution changes before commit; summaries record configure/test command outcomes.</verify>
  <acceptance_criteria>
    - No private paths are reintroduced.
    - Correctness docs and test registration agree.
    - Remaining environment limitation, if any, is explicit.
  </acceptance_criteria>
  <done>Phase 2 can be verified and handed to Phase 3 without hidden correctness ambiguity.</done>
</task>

</tasks>

<verification>
- [ ] `rg "Correctness|known-answer|ctest|--kat|smoke" README.md` finds the documented command.
- [ ] `rg "ECB-128|ECB-256|CTR-128|CTR-256|GCM-128|GCM-256" README.md .planning\\codebase\\TESTING.md` finds coverage notes.
- [ ] `rg "not production cryptography|not production" README.md` still finds the production caveat.
- [ ] `ctest --test-dir C:\tmp\cuda-aes-phase2-build --output-on-failure` is run if build succeeds, otherwise failure reason is recorded.
</verification>

<success_criteria>
- TEST-04 and TEST-05 are satisfied.
- README and testing map make correctness status visible before performance claims.
- Phase 3 can focus on benchmark reproducibility rather than basic AES correctness.
</success_criteria>

<output>
After completion, create `.planning/phases/02-correctness-baseline/02-03-SUMMARY.md`
</output>
