---
phase: 01-repository-and-build-foundation
plan: 03
type: execute
wave: 2
depends_on: ["01-01", "01-02"]
files_modified: ["README.md"]
autonomous: true
requirements: [BUILD-03, BUILD-04, BUILD-05]
user_setup: []
must_haves:
  truths:
    - "BUILD-03: README documents a configure/build command matching the repaired CMake path."
    - "BUILD-04: README states CUDA, CMake, compiler, OpenSSL, and platform prerequisites."
    - "BUILD-05: README explains Windows CUDA host compiler setup and how to pass CMAKE_CUDA_HOST_COMPILER if needed."
    - "D-07: README changes are build-focused, not final SEO/performance marketing."
    - "D-08: README warns that GCM correctness blockers are handled in Phase 2 and current GCM output should not be marketed as standard AES-GCM until fixed."
  artifacts:
    - path: "README.md"
      provides: "Accurate build prerequisites and known limitation notes"
      contains: "CMAKE_CUDA_ARCHITECTURES"
  key_links:
    - from: "README.md"
      to: "CMakeLists.txt"
      via: "documented configure/build commands"
      pattern: "cmake -S \\."
---

<objective>
Update build-facing README documentation after the build configuration is repaired.

Purpose: Phase 1 should leave outside contributors with accurate prerequisites, configure commands, and known limitations without overclaiming benchmark correctness.
Output: README build section that matches portable CMake behavior and explicitly flags GCM correctness blockers for Phase 2.
</objective>

<execution_context>
@$HOME/.codex/get-shit-done/workflows/execute-plan.md
@$HOME/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-repository-and-build-foundation/01-CONTEXT.md
@.planning/phases/01-repository-and-build-foundation/01-RESEARCH.md
@.planning/reviews/2026-06-04-main-branch-code-review.md
@CMakeLists.txt
@README.md
</context>

<threat_model>
Documentation can create security and credibility risk if it overstates cryptographic correctness.

Mitigations in this plan:
- Keep README claims factual and build-focused.
- State that GCM correctness/authentication findings are known blockers for Phase 2.
- Avoid "production-ready crypto library" wording.
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite prerequisites and configure commands</name>
  <files>README.md</files>
  <read_first>README.md, CMakeLists.txt, .planning/phases/01-repository-and-build-foundation/01-CONTEXT.md</read_first>
  <action>
Update README build instructions to match the repaired CMake configuration. Include prerequisites: NVIDIA CUDA Toolkit with `nvcc`, CMake 3.28 or newer unless CMakeLists changes that, a CUDA-compatible host compiler, and OpenSSL discoverable by CMake. Include a Release configure command using `cmake -S . -B build -DCMAKE_BUILD_TYPE=Release -DCMAKE_CUDA_ARCHITECTURES=86` and a build command using `cmake --build build --config Release`. Mention that users should choose their own architecture value.
  </action>
  <verify>rg "CMAKE_CUDA_ARCHITECTURES|cmake -S \\. -B|cmake --build|OpenSSL|CUDA Toolkit" README.md finds the updated build docs.</verify>
  <acceptance_criteria>
    - README no longer only shows Linux `make -j$(nproc)` as the build path.
    - README includes CMake configure and build commands.
    - README explains `CMAKE_CUDA_ARCHITECTURES`.
  </acceptance_criteria>
  <done>Outside contributors can follow README build commands.</done>
</task>

<task type="auto">
  <name>Task 2: Document Windows CUDA host compiler troubleshooting</name>
  <files>README.md</files>
  <read_first>README.md, .planning/reviews/2026-06-04-main-branch-code-review.md</read_first>
  <action>
Add a concise troubleshooting note for the known `nvcc cannot find cl.exe` failure. Tell Windows users to run from a Visual Studio Developer Command Prompt or pass `-DCMAKE_CUDA_HOST_COMPILER=path\\to\\cl.exe` during configure. Do not include any maintainer-specific compiler path.
  </action>
  <verify>rg "cl\\.exe|Visual Studio Developer|CMAKE_CUDA_HOST_COMPILER" README.md finds the troubleshooting note; rg "C:/Users/efebasol|Path/To/VisualStudio" README.md returns no matches.</verify>
  <acceptance_criteria>
    - Known review failure has an actionable README note.
    - No private local path appears in README.
  </acceptance_criteria>
  <done>CUDA host compiler setup is documented for contributors.</done>
</task>

<task type="auto">
  <name>Task 3: Add known limitations without SEO polish</name>
  <files>README.md</files>
  <read_first>README.md, .planning/reviews/2026-06-04-main-branch-code-review.md, .planning/PROJECT.md</read_first>
  <action>
Add a short "Current status / known limitations" section explaining that this is a benchmark/research repository, not a production cryptography library. State that AES-GCM correctness/authentication review findings are scheduled for Phase 2, so current GCM benchmark output must not be treated as standards-compliant AES-GCM until fixed and tested. Keep wording factual and restrained; do not add final SEO marketing copy in Phase 1.
  </action>
  <verify>rg "known limitations|Phase 2|GCM|not a production cryptography library|benchmark" README.md finds the limitation note.</verify>
  <acceptance_criteria>
    - README does not overclaim GCM correctness.
    - README keeps Phase 1 scope build-focused.
  </acceptance_criteria>
  <done>README is accurate enough for public build foundation work.</done>
</task>

</tasks>

<verification>
- [ ] `rg "CMAKE_CUDA_ARCHITECTURES|CMAKE_CUDA_HOST_COMPILER|OpenSSL|CUDA Toolkit" README.md` finds build prerequisite content.
- [ ] `rg "C:/Users/efebasol|Path/To/VisualStudio" README.md` returns no matches.
- [ ] README includes a known limitation note for GCM correctness/authentication blockers.
</verification>

<success_criteria>
- All tasks completed.
- README build docs match repaired CMake behavior.
- README documents CUDA host compiler troubleshooting.
- README does not make final benchmark/SEO claims before correctness phases.
</success_criteria>

<output>
After completion, create `.planning/phases/01-repository-and-build-foundation/01-03-SUMMARY.md`
</output>
